/**
 * @file wifi_creds.h
 * @brief Credentials for wifi
 *
 * You can enter your wifi credentials here so that they are transferred to
 * non-volatile memory the first time you run the firmware.
 *
 * You can always override the settings at any time by using the serial (over
 * USB) connection and the commands SSID and PASSWD. The new credentials you
 * enter that way will be saved to non-volatile memory.
 */
#define WIFI_SSID     ""
#define WIFI_PASSWORD ""
